# Local dev tooling — not part of the deployed site
.mcp.json
design-system/
.playwright-mcp/

# OS / editor cruft
.DS_Store
Thumbs.db

# Never commit real secrets
.env
.env.local

# Node dependencies (verify-collector/) — installed via npm, not shipped in git
node_modules/
